# pi installs git packages with plain `npm install`.
# The extension only uses `import type` from @earendil-works/pi-coding-agent
# (erased at runtime; pi provides the real modules), so skip installing
# peer/dev dependencies to keep installs lean.
omit=dev
legacy-peer-deps=true
